# .streamlit/secrets.toml
# このファイルをコピーして「secrets.toml」という名前に変更し、
# Google CloudでダウンロードしたJSONの中身を貼り付けてください。

[connections.gsheets]
spreadsheet = "https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID_HERE"
worksheet = "books"

# 以下の type 〜 client_x509_cert_url までは、
# ダウンロードした JSON ファイルの中身をそのままコピペします。
# ※ TOML形式なので、全ての行をダブルクォーテーションで囲む必要がありますが、
# Streamlit CloudのSecrets設定画面ではJSONをそのまま貼れる欄があります。

# ローカルで動かす場合 (secrets.toml) は以下のように書きます：
type = "service_account"
project_id = "..."
private_key_id = "..."
private_key = "-----BEGIN PRIVATE KEY-----\n..."
client_email = "..."
client_id = "..."
auth_uri = "https://accounts.google.com/o/oauth2/auth"
token_uri = "https://oauth2.googleapis.com/token"
auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
client_x509_cert_url = "..."
